spring:
  application:
    name: ngcloud-client
server:
  port: 1113

# 服务注册
eureka:
  client:
    #register-with-eureka: false
    #fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

